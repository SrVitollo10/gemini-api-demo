# Gemini API Demo (Python)

## Setup
1. Create a virtual environment
2. Install dependencies:
   pip install -r requirements.txt

3. Create a .env file:
   GEMINI_API_KEY=YOUR_KEY

4. Run:
   python app_gemini.py